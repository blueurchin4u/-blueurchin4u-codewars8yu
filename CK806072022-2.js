/*
Very simple, given an integer or a floating-point number, find its opposite.

1. Create a function. 
2. Return value * a negative, as a positive x negative = a negative, and a negative x negative = a positive.
*/

function opposite(number) {
    return -(number) 
   }