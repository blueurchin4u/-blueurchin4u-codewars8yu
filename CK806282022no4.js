/*
Sum The Strings

Create a function that takes 2 integers in form of a string as an input, and outputs the sum (also as a string):

1. Create function w/ 2 parameters
2.  Use number constructor to convert params to numbers, and return with + '' to output sum as a string
*/

function sumStr(a,b) {
    let x = Number(a)+  Number(b)
    return x + ''
  }
